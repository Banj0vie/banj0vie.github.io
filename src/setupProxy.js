const { createProxyMiddleware } = require('http-proxy-middleware');

module.exports = function(app) {
  // This file is used to configure webpack dev server
  // The polyfills are handled by the webpack configuration in package.json
};

// On-chain sage / weekly wage / weekly harvest removed — stubs.
export const useSage = () => ({
  sageData: null,
  fetchSage: async () => null,
  claimWeeklyWage: async () => null,
  claimWeeklyHarvest: async () => null,
});
export default useSage;
